% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mine-deployed.R
\name{mine_reservoir}
\alias{mine_reservoir}
\title{Mine stationary accelerometer windows via reservoir sampling}
\usage{
mine_reservoir(
  object,
  data_col = NULL,
  ts_col = NULL,
  axes = NULL,
  window_sec = 5,
  sr = NULL,
  sr_tol = 0.5,
  vedba_thresh = 0.05,
  var_thresh = 2e-04,
  mag_thresh = 0.8,
  min_samples_per_window = 3,
  reservoir_size = 4000,
  write_to_file = FALSE,
  slot_size = NULL,
  scratch_path = "./tmp",
  overwrite = FALSE,
  rng_seed = 42,
  chunk_size = 10000,
  delim = ",",
  sep = " ",
  tz = "UTC"
)
}
\arguments{
\item{object}{burst object or path to a burst-format CSV file.}

\item{data_col}{Name of the column holding burst data cells.}

\item{ts_col}{Name of the column holding per-row timestamps.}

\item{axes}{Axis specification.}

\item{window_sec}{Sliding window length in seconds.}

\item{sr}{Nominal sampling rate (Hz). If \code{NULL}, estimated as the
median per-row rate over the first chunk.}

\item{sr_tol}{Maximum allowed deviation of a row's derived rate from
\code{sr} before its samples are discarded and the window is reset.}

\item{vedba_thresh, var_thresh, mag_thresh}{Stationarity thresholds (VeDBA,
mean per-axis variance, minimum window mean-vector magnitude).}

\item{min_samples_per_window}{Minimum samples required before a window
is evaluated.}

\item{reservoir_size}{Maximum number of windows retained.}

\item{write_to_file}{If TRUE, writes the reservoir to an external
scratch file.}

\item{slot_size}{Fixed sample capacity per scratch-file record; estimated
from \code{window_sec} and \code{sr} if \code{NULL}.}

\item{scratch_path}{Path for the scratch binary file.}

\item{overwrite}{if TRUE, the scratch file is overwritten when present.}

\item{rng_seed}{rng seed for repeatability purposes.}

\item{chunk_size}{Rows read per chunk.}

\item{delim}{CSV delimiter.}

\item{sep}{Separator used within burst data cells.}

\item{tz}{Timezone used when parsing non-numeric timestamps.}
}
\value{
A list with reservoir mean vectors (\code{means}), counts, the
scratch file path/slot size needed for reconstruction, and mining
metadata (\code{nominal_sr}, \code{axes}, etc).
}
\description{
Streams a burst-format accelerometer CSV in chunks, reconstructs
per-sample timestamps (deriving each row's sampling rate from its
timestamp and the next row's), and evaluates a time-based sliding window
spanning burst-row boundaries for stationarity (VeDBA, mean per-axis
variance, and minimum magnitude). Passing windows are kept via uniform
(Vitter) reservoir sampling: only mean vectors stay in memory, while raw
window samples are written to a fixed-record scratch binary file for
later retrieval by slot offset.
}
