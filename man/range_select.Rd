% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range-select.R
\name{range_select}
\alias{range_select}
\alias{range_select.matrix}
\title{Select and annotate accelerometery bursts}
\usage{
range_select(acc, axes = NULL, annot = NULL, ...)

\method{range_select}{matrix}(acc, axes = NULL, annot = NULL, ...)
}
\arguments{
\item{acc}{An object of class \code{accelerometery}, \code{matrix}, or
\code{data.frame}}

\item{axes}{A string composed of any non-repeating combination of "x", "y",
"z" (e.g. "xyz", "xz", "y") or \code{NULL}. Columns must be present in
\code{acc}.}

\item{annot}{A data.frame of previously collected (labeled) ranges. It needs
follow the same structure as the output of this same function.}

\item{...}{passed on to methods}
}
\value{
A data.frame of (labeled) ranges with columns: label,
xmin and xmax (start and end time as shown on the plot), imin and imax
(start and end row indices for the range).
}
\description{
Opens an interactive Shiny app that relies on a plotly device to let the
user select and label x-axis ranges.
}
\details{
Use the plotly tools to zoom in and out. Use the box select tool to
highlight a range by click and drag. This function is designed to be used
as a step in bench calibration of devices. Large amounts of data
(most deployment situations) might be slow to plot or cause errors. In such
cases, consider downsampling (e.g. with \code{\link[=downsample]{downsample()}}).
}
\seealso{
\code{\link[=downsample]{downsample()}}, \code{\link[=cal_accel]{cal_accel()}}
}
