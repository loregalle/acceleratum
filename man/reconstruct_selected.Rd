% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mine-deployed.R
\name{reconstruct_selected}
\alias{reconstruct_selected}
\title{Reconstruct raw sample data for selected reservoir windows}
\usage{
reconstruct_selected(reservoir, selected_idx, delete_scratch = FALSE)
}
\arguments{
\item{reservoir}{A reservoir object as returned by \code{mine_reservoir()}.}

\item{selected_idx}{Reservoir slot indices to reconstruct (e.g. from
\code{select_fps()$selected_idx}).}

\item{delete_scratch}{If \code{TRUE}, delete the scratch file after
reading. Default \code{FALSE}, since a reservoir's scratch file may be
read from more than once.}
}
\value{
A list, one element per selected index, each with
\code{timestamp}, \code{data} (raw sample matrix), and \code{source_row}.
}
\description{
Reads raw sample data for the given reservoir slots directly from the
scratch binary file by known byte offset (slot index -> offset), with no
second pass over the original CSV.
}
